apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zeroconnectapi.fullname" . }}-test-connection"
  labels:
    {{- include "zeroconnectapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zeroconnectapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
